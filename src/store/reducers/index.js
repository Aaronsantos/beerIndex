
const initialState = {
    beers: [], 
    loaded: false, 
    next: false ,
    error: false,
    selected: false, 
}

const rootReducer = (state = initialState, action) {

    return state 
}

export default rootReducer